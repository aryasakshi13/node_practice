# require 

require function is build in nodejs for import

# Exports 

Default Exports

# package name in require function
 if in require package we give the package name as it is then it will on their node directory.

 require("fs");

 if we give./ then find their current directory

 require("./math")

 # Modules
 Modules in Node.js are reusable blocks of code that help organize, manage, and separate functionality within an application, preventing global namespace pollution. 
  
 # fs 
 module "fs"
The node:fs module enables interacting with the file system in a way modeled on standard POSIX functions.